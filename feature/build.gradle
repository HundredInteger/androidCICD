plugins {
    id 'com.android.library'
}

apply from: "../base-build.gradle"

android {
    namespace 'io.android_cicd.feature'
}

dependencies {
    implementation(project(path: ':core'))
    implementation(project(path: ':shared'))
}